spring:
  datasource:
    legal:
      jdbc-url: jdbc:postgresql://host.docker.internal:5433/payment_system_legal
      username: LdKrlv
      password: 0987765
      driver-class-name: org.postgresql.Driver
    individual:
      jdbc-url: jdbc:postgresql://host.docker.internal:5432/payment_system_individual
      username: LdKrlv
      password: 0987765
      driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: false

#  artemis:
#    host: artemis
#    port: 61616
#    user: artemis
#    password: simetraehcapa
#    mode: native
#    broker-url: "tcp://host.docker.internal:61616?reconnectAttempts=-1"

  artemis:
    host: artemis
    port: 61616
    user: artemis
    password: simetraehcapa
    mode: native
    #    broker-url: "tcp://payment_artemis:61616?reconnectAttempts=-1"
    broker-url: ${SPRING_ARTEMIS_BROKER_URL:tcp://localhost:61616?reconnectAttempts=-1}

  logging:
    level:
      org.springframework.jms: DEBUG

server:
  port: 8080

# Свои флаги (по умолчанию всё выкл)
app:
  liquibase:
    enabled: true
    legal-enabled: true
    individual-enabled: true
